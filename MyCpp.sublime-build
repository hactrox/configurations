{
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.c, source.c++, source.cpp",
	"shell": true,
	/* Quit after finish */
	// "cmd": ["g++", "${file}", "-o", "${file_path}/${file_base_name}","-Wall", "&&","start"," ","${file_path}/${file_base_name}.exe","${file_base_name}"],
	/* Press any key after finish */
	"cmd": ["g++", "${file}", "-o", "${file_path}/${file_base_name}","-Wall", "&&","start","cmd","/c", "${file_path}/${file_base_name}.exe &pause"]
}